---
---

<section class="hero">
    <div class="hero__content">
      <h1 class="hero__title">Transforma tu pasión en profesión</h1>
      <p class="hero__subtitle">Descubre nuestros cursos de belleza y lleva tu carrera al siguiente nivel</p>
      <a href="#cursos" class="hero__cta">Explora nuestros cursos</a>
    </div>
    <div class="hero__image">
      <img src="./src/images/hero-uñas-acrilicas-optimizado.jpg" alt="Cursos de belleza" width="1920" height="1080">
    </div>
  </section>
  
  <style>
    .hero {
      position: relative;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }
  
    .hero__content {
      position: relative;
      z-index: 2;
      text-align: center;
      padding: 2rem;
      background: rgba(74, 20, 140, 0.7);
      border-radius: 10px;
      transition: transform 0.3s ease;
    }
  
    .hero__content:hover {
      transform: scale(1.05);
    }
  
    .hero__title {
      font-size: 3rem;
      margin-bottom: 1rem;
      color: var(--color-text);
    }
  
    .hero__subtitle {
      font-size: 1.5rem;
      margin-bottom: 2rem;
      color: var(--color-text);
    }
  
    .hero__cta {
      display: inline-block;
      padding: 1rem 2rem;
      background: var(--color-accent);
      color: var(--color-text);
      text-decoration: none;
      border-radius: 50px;
      font-weight: bold;
      transition: all 0.3s ease;
    }
  
    .hero__cta:hover {
      background: #1ea952;
      transform: translateY(-5px);
      box-shadow: 0 5px 15px rgba(37, 211, 102, 0.3);
    }
  
    .hero__image {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 1;
    }
  
    .hero__image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center;
    }
  
    @media (max-width: 768px) {
      .hero__title {
        font-size: 2rem;
      }
  
      .hero__subtitle {
        font-size: 1.2rem;
      }
    }
  </style>