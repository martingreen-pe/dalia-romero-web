---
const preguntas = [
  {
    pregunta: "¿Cuál es la duración de los cursos?",
    respuesta: "La duración de nuestros cursos varía según la técnica. En general, los cursos tienen una duración de 4 a 8 semanas, con clases de 3 a 4 horas por sesión. Puedes elegir entre clases presenciales o en vivo online, según tu preferencia."
  },
  {
    pregunta: "¿Se proporcionan los materiales necesarios?",
    respuesta: "Sí, proporcionamos todos los materiales necesarios para la práctica durante los cursos presenciales. Para las clases online en vivo, te daremos una lista de herramientas recomendadas para que las adquieras antes de comenzar."
  },
  {
    pregunta: "¿Ofrecen certificados al finalizar el curso?",
    respuesta: "Sí, al completar satisfactoriamente el curso, ya sea presencial o online en vivo, recibirás un certificado que acredita las habilidades adquiridas."
  },
  {
    pregunta: "¿Necesito experiencia previa para tomar los cursos?",
    respuesta: "No se requiere experiencia previa. Nuestros cursos, tanto presenciales como online en vivo, están diseñados tanto para principiantes como para profesionales que desean mejorar sus habilidades."
  },
  {
    pregunta: "¿Puedo acceder a las clases si no puedo asistir en vivo?",
    respuesta: "Sí, todas nuestras clases online en vivo son grabadas, por lo que podrás acceder a ellas en cualquier momento si no puedes asistir a la sesión en vivo."
  }
];

---

<section class="preguntas-frecuentes">
  <h2 class="preguntas-frecuentes__titulo">Preguntas Frecuentes</h2>
  <div class="preguntas-frecuentes__lista">
    {preguntas.map((item, index) => (
      <details class="pregunta-item">
        <summary class="pregunta-item__pregunta">{item.pregunta}</summary>
        <p class="pregunta-item__respuesta">{item.respuesta}</p>
      </details>
    ))}
  </div>
</section>

<style>
  .preguntas-frecuentes {
    margin-top: 3.5rem;
    padding: 4rem 1rem;
    background: var(--gradient-primary);
  }

  .preguntas-frecuentes__titulo {
    text-align: center;
    font-size: 2.5rem;
    margin-bottom: 2rem;
    color: var(--color-text);
  }

  .preguntas-frecuentes__lista {
    max-width: 800px;
    margin: 0 auto;
  }

  .pregunta-item {
    background: rgba(255, 255, 255, 0.1);
    border-radius: 10px;
    margin-bottom: 1rem;
    transition: background-color 0.3s ease;
  }

  .pregunta-item__pregunta {
    font-size: 1.2rem;
    padding: 1rem;
    cursor: pointer;
    color: var(--color-text);
  }

  .pregunta-item__respuesta {
    padding: 0 1rem 1rem;
    color: var(--color-text);
  }

  .pregunta-item:hover {
    background: rgba(255, 255, 255, 0.2);
  }

  details[open] summary {
    font-weight: bold;
  }

  @media (min-width: 768px) {
    .pregunta-item__pregunta {
      font-size: 1.3rem;
    }
  }
</style>

<script>
  const detalles = document.querySelectorAll('details');
  detalles.forEach((detalle) => {
    detalle.addEventListener('toggle', () => {
      if (detalle.open) {
        detalles.forEach((d) => {
          if (d !== detalle) d.open = false;
        });
      }
    });
  });
</script>