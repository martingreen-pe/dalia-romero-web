---
// Definir la información de "Sobre Dalia Romero" en formato JSON
const sobreMi = {
  titulo: "Sobre Dalia Romero",
  imagen: "/images/dalia-romero.png",
  descripcion: [
    "Soy Dalia Romero, artista venezolana con más de 5 años de experiencia en el nail art. He fusionado mi pasión por el arte y la belleza para crear una academia donde tú también puedes desarrollar tu talento. He ganado varios concursos internacionales y ahora quiero ayudarte a dar tus primeros pasos en este maravilloso mundo de las uñas. ¡Juntas, haremos de tus manos una obra de arte!"
  ]
};
---

<section class="sobre-mi">
  <div class="sobre-mi__container">
    <img src={sobreMi.imagen} alt={sobreMi.titulo} class="sobre-mi__imagen">
    <div class="sobre-mi__content">
      <h2 class="sobre-mi__titulo">{sobreMi.titulo}</h2>
      {sobreMi.descripcion.map((texto) => (
        <p class="sobre-mi__texto">{texto}</p>
      ))}
    </div>
  </div>
</section>

<style>
  .sobre-mi {
    margin-top: 2rem;
    padding: 1.5rem 1rem;
    background: var(--gradient-primary); /* Usando el degradado desde global.css */
    color: var(--color-white); /* Texto en blanco para contraste */
  }

  .sobre-mi__container {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 2rem;
    border-radius: 10px; /* Bordes redondeados */
    background: none; /* Sin fondo adicional, el degradado ya cubre */
  }

  .sobre-mi__imagen {
    width: 200px;
    height: 200px;
    border-radius: 50%;
    object-fit: cover;
    margin-bottom: 2rem;
    border: 4px solid var(--color-accent);
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Sombra en la imagen */
  }

  .sobre-mi__content {
    text-align: center;
  }

  .sobre-mi__titulo {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: var(--color-text); /* Color de texto oscuro */
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .sobre-mi__texto {
    font-size: 1.1rem;
    line-height: 1.8;
    margin-bottom: 1rem;
    color: var(--color-text); /* Color oscuro para mejor contraste */
    opacity: 0.9;
  }

  @media (min-width: 768px) {
    .sobre-mi__container {
      flex-direction: row;
      align-items: flex-start;
      padding: 3rem;
    }

    .sobre-mi__imagen {
      margin-right: 2rem;
      margin-bottom: 0;
    }

    .sobre-mi__content {
      text-align: left;
    }
  }
</style>

