---
const testimonios = [
  {
    nombre: "María González",
    curso: "Curso de Softgel",
    testimonio: "El curso de Dalia me ha dado la confianza y las habilidades que necesitaba para iniciar mi propio negocio de manicura. ¡Altamente recomendado!",
  },
  {
    nombre: "Laura Pérez",
    curso: "Curso de Uñas Acrílicas",
    testimonio: "La atención al detalle y la paciencia de Dalia como instructora son incomparables. He mejorado significativamente mis técnicas gracias a su curso.",
  },
  {
    nombre: "Ana Martínez",
    curso: "Curso de Manicura Rusa",
    testimonio: "Aprender la técnica de manicura rusa con Dalia ha sido una experiencia transformadora. Mis clientes están encantados con los resultados.",
  },
];
---

<section class="testimonios">
  <h2 class="testimonios__titulo">Lo que dicen nuestros estudiantes</h2>
  <div class="testimonios__grid">
    {testimonios.map((testimonio) => (
      <div class="testimonio-card">
        <p class="testimonio-card__texto">"{testimonio.testimonio}"</p>
        <p class="testimonio-card__autor">{testimonio.nombre}</p>
        <p class="testimonio-card__curso">{testimonio.curso}</p>
      </div>
    ))}
  </div>
</section>

<style>
  .testimonios {
    margin-top: 3.5rem;
    padding: 4rem 1rem;
    background: var(--gradient-primary); /* Usando el degradado definido globalmente */
  }

  .testimonios__titulo {
    text-align: center;
    font-size: 2.5rem;
    margin-bottom: 2rem;
    color: var(--color-dark-gray); /* Color oscuro para contraste */
    text-transform: uppercase;
    letter-spacing: 1px; /* Espaciado de letras para el título */
  }

  .testimonios__grid {
    display: grid;
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .testimonio-card {
    background: rgba(255, 255, 255, 0.9); /* Fondo blanco con un poco de transparencia */
    border-radius: 10px;
    padding: 2rem;
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease; /* Efecto suave de hover */
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); /* Sombra ligera */
  }

  .testimonio-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2); /* Sombra más fuerte en hover */
  }

  .testimonio-card__texto {
    font-size: 1rem;
    line-height: 1.8;
    margin-bottom: 1.5rem;
    color: var(--color-dark-gray); /* Color oscuro para el texto */
    font-style: italic; /* Texto en cursiva para destacar el testimonio */
  }

  .testimonio-card__autor {
    font-weight: bold;
    color: var(--color-accent); /* Color de acento para el nombre */
    margin-bottom: 0.5rem;
  }

  .testimonio-card__curso {
    font-size: 0.9rem;
    color: var(--color-medium-gray); /* Color más suave para el curso */
  }

  @media (min-width: 768px) {
    .testimonios__grid {
      grid-template-columns: repeat(2, 1fr); /* Dos columnas en pantallas medianas */
    }
  }

  @media (min-width: 1024px) {
    .testimonios__grid {
      grid-template-columns: repeat(3, 1fr); /* Tres columnas en pantallas grandes */
    }
  }
</style>
