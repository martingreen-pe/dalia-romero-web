---
---

<section class="cta-whatsapp">
    <div class="cta-content">
      <h2>¿Listo para dar el siguiente paso?</h2>
      <p>Conéctate con nosotros ahora y descubre cómo podemos ayudarte a alcanzar tus metas.</p>
      <a href="https://wa.me/TUNUMERODEWHATSAPP" class="cta-button" target="_blank" rel="noopener noreferrer" id="ctaButton">
        Chatea con nosotros en WhatsApp
      </a>
    </div>
  </section>
  
  <style>
    .cta-whatsapp {
      margin-top: 3.5rem;
      background: var(--gradient-primary); /* Usando el degradado global */
      padding: 4rem 2rem;
      text-align: center;
      color: var(--color-text);
      transition: transform 0.3s ease;
    }
  
    .cta-content {
      max-width: 600px;
      margin: 0 auto;
    }
  
    h2 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      transition: color 0.3s ease;
      color: var(--color-dark-gray); /* Color oscuro para contraste */
      text-transform: uppercase;
      letter-spacing: 1px;
    }
  
    p {
      font-size: 1.2rem;
      margin-bottom: 2rem;
      color: var(--color-medium-gray);
      transition: opacity 0.3s ease;
    }
  
    .cta-button {
      display: inline-block;
      background: var(--color-accent);
      color: var(--color-white); /* Texto en blanco */
      padding: 1rem 2rem;
      border-radius: 50px;
      font-size: 1.2rem;
      text-decoration: none;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2); /* Sombra ligera */
    }
  
    .cta-button:before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 0;
      height: 0;
      background: rgba(255, 255, 255, 0.2); /* Efecto de luz circular en hover */
      border-radius: 50%;
      transform: translate(-50%, -50%);
      transition: width 0.6s ease, height 0.6s ease;
    }
  
    .cta-button:hover:before {
      width: 300px;
      height: 300px;
    }
  
    .cta-button:hover {
      background: var(--color-accent-light); /* Cambia el fondo en hover */
      transform: translateY(-3px);
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
    }
  
    @media (min-width: 768px) {
      h2 {
        font-size: 3rem; /* Aumenta el tamaño del título en pantallas más grandes */
      }
  
      p {
        font-size: 1.4rem; /* Aumenta el tamaño del texto en pantallas más grandes */
      }
  
      .cta-button {
        font-size: 1.4rem; /* Aumenta el tamaño del botón en pantallas grandes */
      }
    }
  </style>
  
  
  
  
  <script>
    const ctaSection = document.querySelector('.cta-whatsapp') as HTMLElement;
    const ctaButton = document.getElementById('ctaButton') as HTMLAnchorElement;
    const ctaTitle = document.querySelector('.cta-whatsapp h2') as HTMLElement;
    const ctaText = document.querySelector('.cta-whatsapp p') as HTMLElement;

    if (ctaSection && ctaButton && ctaTitle && ctaText) {
      ctaSection.addEventListener('mouseenter', () => {
        ctaSection.style.transform = 'scale(1.02)';
        ctaTitle.style.color = '#25D366';
        ctaText.style.opacity = '0.8';
      });

      ctaSection.addEventListener('mouseleave', () => {
        ctaSection.style.transform = 'scale(1)';
        ctaTitle.style.color = '';
        ctaText.style.opacity = '1';
      });

      ctaButton.addEventListener('mouseenter', () => {
        ctaButton.style.transform = 'translateY(-5px) scale(1.05)';
      });

      ctaButton.addEventListener('mouseleave', () => {
        ctaButton.style.transform = '';
      });

      ctaButton.addEventListener('click', (e) => {
        e.preventDefault();
        ctaButton.style.transform = 'scale(0.95)';
        setTimeout(() => {
          window.open(ctaButton.href, '_blank');
          ctaButton.style.transform = '';
        }, 300);
      });
    }
</script>